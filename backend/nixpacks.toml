[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = ["cd spotifyClone && dotnet publish -c Release -o out"]

[phases.start]
cmd = "cd spotifyClone/out && dotnet spotifyClone.dll"

[variables]
ASPNETCORE_ENVIRONMENT = "Production"
